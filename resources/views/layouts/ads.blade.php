
	<section id="advertisement">
		<div class="container">
			<a href="{{ $slideads2->link }}">
				<img src="upload/slide_ads/{{ $slideads2->hinh }}" alt="{{ $slideads2->noidung }}" />
			</a>
		</div>
	</section>